<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- Enable component scanning -->
    <context:component-scan base-package="com.dailyrecycling.service" />

    <!-- Database Configuration -->
    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="org.postgresql.Driver" />
        <property name="url" value="${db.url:jdbc:postgresql://localhost:5432/pcb}" />
        <property name="username" value="${db.username:your_username}" />
        <property name="password" value="${db.password:your_password}" />
    </bean>

    <!-- JdbcTemplate -->
    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <property name="dataSource" ref="dataSource" />
    </bean>

    <!-- Email Configuration -->
    <bean id="mailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl">
        <property name="host" value="smtp.gmail.com" />
        <property name="port" value="587" />
        <property name="username" value="your_email@gmail.com" />
        <property name="password" value="your_app_password" />
        <property name="javaMailProperties">
            <props>
                <prop key="mail.smtp.auth">true</prop>
                <prop key="mail.smtp.starttls.enable">true</prop>
            </props>
        </property>
    </bean>

    <!-- Service Beans -->
    <bean id="databaseService" class="com.dailyrecycling.service.DatabaseService">
        <property name="jdbcTemplate" ref="jdbcTemplate" />
    </bean>

    <bean id="fixedWidthParser" class="com.dailyrecycling.service.FixedWidthParser" />

    <bean id="errorCorrectionService" class="com.dailyrecycling.service.ErrorCorrectionService">
        <property name="databaseService" ref="databaseService" />
        <property name="fixedWidthParser" ref="fixedWidthParser" />
        <property name="emailService" ref="emailService" />
    </bean>

    <bean id="emailService" class="com.dailyrecycling.service.EmailService">
        <property name="mailSender" ref="mailSender" />
        <property name="fromEmail" value="noreply@dailyrecycling.com" />
    </bean>

    <!-- Action Beans for Struts-Spring Integration -->
    <bean id="recyclingAction" class="com.dailyrecycling.action.RecyclingAction" scope="prototype">
        <property name="fixedWidthParser" ref="fixedWidthParser" />
        <property name="errorCorrectionService" ref="errorCorrectionService" />
    </bean>

</beans>

